<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Authorize Access - BSIM Open Banking</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .container {
      background: white;
      border-radius: 16px;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
      width: 100%;
      max-width: 520px;
      padding: 40px;
    }
    .header {
      text-align: center;
      margin-bottom: 32px;
    }
    .client-logo {
      width: 64px;
      height: 64px;
      border-radius: 12px;
      margin-bottom: 16px;
      object-fit: cover;
    }
    .client-placeholder {
      width: 64px;
      height: 64px;
      border-radius: 12px;
      background: #e5e7eb;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 16px;
      font-size: 24px;
      color: #9ca3af;
    }
    h1 {
      font-size: 24px;
      font-weight: 600;
      color: #111827;
      margin-bottom: 8px;
    }
    .subtitle {
      color: #6b7280;
      font-size: 14px;
    }
    .client-name {
      font-weight: 600;
      color: #4f46e5;
    }
    .user-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: #f3f4f6;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 14px;
      color: #374151;
      margin-top: 12px;
    }
    .section {
      margin-bottom: 24px;
    }
    .section-title {
      font-size: 14px;
      font-weight: 600;
      color: #374151;
      margin-bottom: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .permissions {
      list-style: none;
    }
    .permission {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 12px;
      background: #f9fafb;
      border-radius: 8px;
      margin-bottom: 8px;
    }
    .permission-icon {
      width: 20px;
      height: 20px;
      color: #4f46e5;
      flex-shrink: 0;
      margin-top: 2px;
    }
    .permission-text {
      font-size: 14px;
      color: #374151;
    }
    .accounts-list {
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      overflow: hidden;
    }
    .account-item {
      display: flex;
      align-items: center;
      padding: 12px 16px;
      border-bottom: 1px solid #e5e7eb;
    }
    .account-item:last-child {
      border-bottom: none;
    }
    .account-item input {
      width: 18px;
      height: 18px;
      margin-right: 12px;
    }
    .account-info {
      flex: 1;
    }
    .account-number {
      font-family: monospace;
      font-size: 14px;
      color: #374151;
    }
    .account-balance {
      font-size: 12px;
      color: #6b7280;
    }
    .buttons {
      display: flex;
      gap: 12px;
      margin-top: 24px;
    }
    button {
      flex: 1;
      padding: 12px;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
    }
    .btn-cancel {
      background: white;
      border: 1px solid #d1d5db;
      color: #374151;
    }
    .btn-cancel:hover {
      background: #f9fafb;
    }
    .btn-authorize {
      background: #4f46e5;
      border: none;
      color: white;
    }
    .btn-authorize:hover {
      background: #4338ca;
    }
    .warning {
      background: #fffbeb;
      border: 1px solid #fde68a;
      color: #92400e;
      padding: 12px 16px;
      border-radius: 8px;
      font-size: 13px;
      margin-top: 16px;
    }
    .info {
      text-align: center;
      font-size: 12px;
      color: #9ca3af;
      margin-top: 16px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <% if (clientLogo) { %>
        <img src="<%= clientLogo %>" alt="<%= clientName %>" class="client-logo">
      <% } else { %>
        <div class="client-placeholder">🏦</div>
      <% } %>
      <h1>Authorize Access</h1>
      <p class="subtitle">
        <span class="client-name"><%= clientName %></span> wants to access your banking data
      </p>
      <div class="user-badge">
        <span>👤</span>
        <span>Signed in as <%= user.email %></span>
      </div>
    </div>

    <form method="post" action="/interaction/<%= uid %>/confirm">
      <div class="section">
        <div class="section-title">Permissions Requested</div>
        <ul class="permissions">
          <% scopes.forEach(function(scope) { %>
            <li class="permission">
              <svg class="permission-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              <span class="permission-text"><%= scope.description %></span>
            </li>
          <% }); %>
        </ul>
      </div>

      <% if (accounts.length > 0) { %>
        <div class="section">
          <div class="section-title">Select Accounts to Share</div>
          <div class="accounts-list">
            <% accounts.forEach(function(account) { %>
              <label class="account-item">
                <input type="checkbox" name="selectedAccounts" value="<%= account.id %>" checked>
                <div class="account-info">
                  <div class="account-number">****<%= account.accountNumber.slice(-4) %></div>
                  <div class="account-balance">Balance: $<%= parseFloat(account.balance).toFixed(2) %></div>
                </div>
              </label>
            <% }); %>
          </div>
        </div>
      <% } %>

      <div class="warning">
        <strong>Important:</strong> Only authorize applications you trust. <%= clientName %> will be able to access the selected data until you revoke access.
      </div>

      <div class="buttons">
        <button type="button" class="btn-cancel" onclick="document.getElementById('abort-form').submit()">
          Deny
        </button>
        <button type="submit" class="btn-authorize">
          Authorize
        </button>
      </div>
    </form>

    <form id="abort-form" method="post" action="/interaction/<%= uid %>/abort" style="display: none;"></form>

    <p class="info">
      You can revoke access at any time in your BSIM account settings.
    </p>
  </div>
</body>
</html>
